#include <string>

class Solution {
public:
    int lengthOfLastWord(std::string s) {
        int i = s.length() - 1;

        while (i >= 0 && s[i] == ' '){
            i--;
        }
        
        int lastIndex = i;

        while (i >= 0 && s[i] != ' '){
            i--;
        }
        return lastIndex - i;
    }
};
